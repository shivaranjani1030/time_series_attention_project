# time_series_attention_project
Deep learning–based multivariate time series forecasting with LSTM and self-attention. Features synthetic dataset generation, time-aware preprocessing, hyperparameter tuning, and performance comparison against ARIMA and vanilla LSTM models.
